# Running Locally

1. Make sure the environment variables are configured.
2. Run the project:
   ```bash
   mvn spring-boot:run
   ```
3. Access [http://localhost:8080](http://localhost:8080) in your browser to check if it is working.
