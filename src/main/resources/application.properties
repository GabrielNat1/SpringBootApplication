spring.application.name=spring-boot-project

# Configuração para SQLite3
spring.datasource.url=jdbc:sqlite:database.db
spring.datasource.driver-class-name=org.sqlite.JDBC

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.community.dialect.SQLiteDialect

# Admin configuration (username only - password must be set via environment variable)
admin.username=admin
# Admin configuration
admin.username=admin
# admin.password should be set via environment variable ADMIN_PASSWORD
